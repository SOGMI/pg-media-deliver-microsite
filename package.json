{
  "name": "pg-2018-audio-delivery",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others \"npm run watch-css\" \"hugo server\"",
    "watch-css": "node-sass --watch styles/main.scss -o static"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "dependencies": {
    "bulma": "^0.7.2",
    "node-sass": "^4.11.0",
    "slugify": "^1.3.4"
  }
}
